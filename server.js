//call PORT and specify number
const PORT = process.env.PORT || 3001;

//require variables from package-lock.json
const fs = require('fs');
const path = require('path');
const express = require('express');

